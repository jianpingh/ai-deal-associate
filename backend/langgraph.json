{
    "python_version": "3.12",
    "title": "AI Deal Associate",
    "description": "An AI agent for deal processing.",
    "dependencies": ["."],
    "dockerfile_lines": [
        "RUN apt-get update && apt-get install -y libmagic-dev poppler-utils tesseract-ocr"
    ],
    "graphs": {
        "agent": "./deal_agent/agent.py:app"
    },
    "env": ".env"
}
